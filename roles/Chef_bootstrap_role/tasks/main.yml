---

  - name: bootstrapping the nodes
    command: knife bootstrap "{{ item }}" --ssh-user "{{ username }}" --ssh-password "{{ password }}" --sudo --use-sudo-password --environment _default --bootstrap-proxy http://proxy.its.hpecorp.net:8080 https_proxy=$http_proxy
    #environment: "{{proxy_env}}"
    with_items: "{{ hostnames }}"
    



